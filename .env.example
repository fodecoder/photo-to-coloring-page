# This project does not currently require any environment variables.
# This file is kept as a placeholder so that if configuration is ever
# needed (for example, to point an optional ML engine at a local model
# cache directory), it is documented here rather than hardcoded.
#
# Copy this file to `.env` and fill in real values if/when a setting
# below is introduced. Never commit a real `.env` file.
